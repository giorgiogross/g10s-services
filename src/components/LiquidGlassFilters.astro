---
// Liquid glass displacement filter — shared across the landing page and
// demo projects. The refraction is driven by a pre-baked displacement
// WebP (/liquid-map.webp) sampled via feImage and fed into
// feDisplacementMap. primitiveUnits="objectBoundingBox" lets the same
// map stretch cleanly across any pill size.
//
// Safari / iOS note: `backdrop-filter: url(#...)` is unsupported, so
// consuming styles must pair the url() filter with a plain
// `-webkit-backdrop-filter: blur() saturate()` fallback.
---

<svg
  width="0"
  height="0"
  aria-hidden="true"
  style="position:absolute;width:0;height:0;overflow:hidden;pointer-events:none;"
>
  <defs>
    <!-- Generous filter region so displaced pixels near the corners are
         still sampled from inside the filter buffer rather than from
         the transparent void outside it (that's the "hole" symptom).
         scale is kept small for the same reason — 0.4 was pushing
         bottom-right pixels ~40% of the element size off the edge. -->
    <filter
      id="g10s-liquid-glass"
      primitiveUnits="objectBoundingBox"
      x="-25%"
      y="-25%"
      width="150%"
      height="150%"
    >
      <feImage
        href="/liquid-map.webp"
        x="0"
        y="0"
        width="100%"
        height="100%"
        result="map"
        preserveAspectRatio="none"
      />
      <feGaussianBlur in="SourceGraphic" stdDeviation="0.01" result="blur" />
      <feDisplacementMap
        in="blur"
        in2="map"
        scale="0.08"
        xChannelSelector="R"
        yChannelSelector="G"
      />
    </filter>
  </defs>
</svg>
